comment:
  layout: "reach, diff, flags, files"
  behavior: default
  require_changes: false
  require_base: no
  require_head: yes
  branches: null
coverage:
  status:
    project:
      default:
        target: 80%
    patch:
      default:
        target: 80%
